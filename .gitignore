tags
test.sh
.luarc.json
nvim

spell/

# nvim-pack-lock.json IS tracked in this fork, as `:help vim.pack-lockfile`
# recommends. The lockfile is the revert path for
# `vim.pack.update(..., { target = 'lockfile' })`, and that path is inert while
# the file is untracked.

.DS_Store

# pi agent runtime state (loops, sessions) - not part of the editor config
.pi/
